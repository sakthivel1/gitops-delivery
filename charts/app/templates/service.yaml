apiVersion: v1
kind: Service
metadata:
  name: {{ include "finapp.fullname" . }}
  labels:
    {{- include "finapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "finapp.selectorLabels" . | nindent 4 }}
